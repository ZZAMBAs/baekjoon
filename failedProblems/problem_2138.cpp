/*
//
// Created by AppDeveloper on 2022-10-25.
#include <iostream>

using namespace std;

int main(){
    int board[10], cmd;
    fill(board, board + 10, 0);

    */
/*  while (cin >> cmd){
        if (cmd >= 10)
            break;

        for (int i = cmd - 1; i <= cmd + 1; ++i) {
            if (i >= 0 && i < 10)
                board[i] = (board[i] + 1) % 2;
        }

        for (int i = 0; i < 10; ++i)
            cout << board[i];
        cout << endl;
    }*//*

    // 시뮬레이션 용 코드

    return 0;
}
*/
